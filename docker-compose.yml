services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - data:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/rideshare.db
    restart: unless-stopped

volumes:
  data:
